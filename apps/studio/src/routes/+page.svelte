<script lang="ts">
  /**
   * Root page: thin shell.
   *
   * The editor is now rendered as a dockview panel (EditorPanel.svelte).
   * This route exists to satisfy SvelteKit routing; the DockLayout in
   * +layout.svelte renders the workspace when this route is active.
   */
</script>

<svelte:head>
  <title>ActOne Studio</title>
</svelte:head>
